# Roadmap

## v0.2
- [ ] CIS Docker Benchmark full ruleset (via docker-bench-security or native checks)
- [ ] Slack/Teams alert on new EOL or license violation
- [ ] VIB SBOM push integration — consume SBOMs generated by VIB instead of re-scanning
- [ ] Per-image exception file (`cib-exceptions.yaml`) for acknowledged violations

## v0.3
- [ ] Kubernetes manifest compliance (check Pod specs against CIS K8s benchmark)
- [ ] SOC 2 control mapping — map CIB checks to relevant SOC 2 controls
- [ ] NIST CSF coverage report export (PDF)

## Backlog
- SPDX SBOM format support alongside CycloneDX
- License compatibility matrix (e.g. Apache-2.0 + GPL-2.0 = incompatible)
- GitHub Actions integration to block PR if new license violation introduced
